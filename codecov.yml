coverage:
  status:
    project:
      default:
        # Compare against the base commit so existing uncovered code does not make every PR fail.
        target: auto
        threshold: 0%
    patch:
      default:
        target: 100%
        threshold: 0%
